[package]
name = "increment"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = { version = "23.0.0-rc.3" }

[dev-dependencies]
soroban-sdk = { version = "23.0.0-rc.3", features = ["testutils"] }
